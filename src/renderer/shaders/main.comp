#version 460
layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout (set = 0, binding = 0) buffer result_ssbo {
  vec4 color[];
};

void main() {
  uint index = gl_GlobalInvocationID.x;
  color[index] = vec4(0.0f, 1.0f, 0.0f, 1.0f);
}